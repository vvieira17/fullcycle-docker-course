# fullcycle-docker-course: Nginx + Node.js + MySQL

Ambiente automatizado com Docker Compose.

## Execução

1. Na pasta raiz do projeto, execute: `docker compose up -d --build`
2. Após subir os containers acesse: `http://localhost:8080`

## Stop
Comando: `docker compose down`
